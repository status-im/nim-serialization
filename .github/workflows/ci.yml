name: CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: read
    uses: status-im/nimbus-common-workflow/.github/workflows/common.yml@main
